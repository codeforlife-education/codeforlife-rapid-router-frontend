/**
 * Pre-filled boilerplate shown in every Python editor - both the editable
 * one (mode "python") and the read-only, Blockly-derived one (mode
 * "blocklyAndPython") - matching the `van` module's `Van` class exposed to
 * student code (see `pyodide.worker.ts`).
 */
export const PYTHON_STARTER_CODE = "from van import Van\n\nmy_van = Van()\n\n"
